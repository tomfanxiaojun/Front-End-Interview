let snack = 'Meow Mix';

function getFood(food) {
	console.log(snack);
    if (food) {
    	
        let snack = 'Friskies';
        return snack;
    }
    return snack;
}

getFood(true); // 'Meow Mix'